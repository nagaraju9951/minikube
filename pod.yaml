apiVersion: v1
kind: Pod
metadata:
  name: apache
  labels:
    app: apache
spec:
  containers:
    - name: apache-container
      image: apache:latest
      imagePullPolicy: Never
      ports:
        - containerPort: 80
